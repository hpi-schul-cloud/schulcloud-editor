dist: xenial
language: node_js
node_js: '10.16'
branches:
  only:
    - develop
    - master
    - /^greenkeeper/.*$/
    - /^(?i:release|hotfix).*$/
jobs:
  include:
    - stage: test
      name: test:mocha
      script: npm test
      cache: npm
      services:
        - mongodb
