/*

	async createHomework(data, params) {
		const { ref } = data;
		if (!(typeof ref === 'object')) {
			throw new BadRequest('No ref key is exist. Please add it with the keys, target and targetModel');
		}
		throw new NotImplemented();
	}

	async moveHomeworks(data, params) {
		throw new NotImplemented();
	}
*/